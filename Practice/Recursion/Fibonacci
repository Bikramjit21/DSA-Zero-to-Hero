Problem Statement:
Given a number n. you have to print the sum of first n numbers of the fibonacci series.

Solution:
#include <bits/stdc++.h>
using namespace std;

int fibo(int n){
    if(n<=1){
        return n;
    }
    else{
        return fibo(n-1)+fibo(n-2);
    }
}
int main()
{
    int n;
    cin>>n;
    cout<<fibo(n);
}
