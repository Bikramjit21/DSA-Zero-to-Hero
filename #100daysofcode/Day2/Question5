Find the union and intersection of the two sorted arrays.

solution:
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,m;
    cin>>n>>m;
    
    set<int> s;
    int a[n],b[m],i;
    for(i=0;i<n;i++){
        cin>>a[i];
        s.insert(a[i]);
    }
    for(i=0;i<m;i++){
        cin>>b[i];
        s.insert(b[i]);
    }
    
    cout<<s.size()<<" "<<endl;
    
    return 0;
}
