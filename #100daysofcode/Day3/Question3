Find the sum of fibonnaci series of a given range.

solution:
#include <iostream>
using namespace std;

int fibo(int n){
    if(n==0 || n==1){
        return n;
    }
    
    return fibo(n-1)+fibo(n-2);
    
}
int main()
{
    cout<<"Enter the range: ";
    int n;
    cin>>n;

    cout<<"Fibonacci series of "<<n<<" range is: "<<fibo(n);
    
    return 0;
}
